# Stub flotte : self-healing des crons (logique dans fleet-kit/self-heal.yml).
# Posé pour le TEST D5 (surveille « Cron test D5 ») — après le test, soit le retirer,
# soit le recâbler si le repo gagne un vrai cron.
name: Self-heal
on:
  workflow_run:
    workflows: ["Cron test D5"]
    types: [completed]
jobs:
  heal:
    uses: Thibaud888/fleet-kit/.github/workflows/self-heal.yml@main
    secrets: inherit
    permissions:
      contents: write
      issues: write
      pull-requests: write
      actions: read
      id-token: write
