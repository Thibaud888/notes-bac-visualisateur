# TEST self-heal (chantier D5 claude-ops) — workflow temporaire, à supprimer après le test.
name: Cron test D5
on:
  workflow_dispatch:
jobs:
  job:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/cron-test.mjs
