#!/usr/bin/env node
// Job de test du self-heal (chantier D5 claude-ops) — à supprimer après le test.
// Contient une coquille volontaire pour faire échouer le cron.
const message = "ping du cron de test";
console.lgo(message);
